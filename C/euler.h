#ifndef EULER_H
#define EULER_H

#include "math.h"
#include "stdio.h"
#include "stdlib.h"

int* sieve(int n) {
  int i,j;
  int primes[n];
  for (i=0;i<n;++i) {
    printf("%i",primes[i]);
  }
  for (i=2;i<=sqrt(n);++i) {
  }
}

#endif
